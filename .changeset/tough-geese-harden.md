---
'@reintersect/load-config': patch
'@reintersect/svelte-language-server': patch
'@reintersect/svelte-check': patch
---

Harden the tsgo engine across native process handling, document lifecycle, configuration and project invalidation, checker output, template diagnostics, and editor feature mapping. Publish the config loader under the Reintersect scope so runtime config invalidation matches the tested implementation.
