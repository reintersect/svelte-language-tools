---
'@reintersect/svelte-load-config': patch
'@reintersect/svelte-language-server': patch
'@reintersect/svelte-check': patch
---

Publish the config runtime as `@reintersect/svelte-load-config` and update the tsgo language server and checker to use the clearer scoped package name.
